import { CanonicalModule } from '@/components/ui/CanonicalModule';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sitecontrol | Land Intelligence OS',
};

export default function SitecontrolPage() {
  return <CanonicalModule title="Sitecontrol" />;
}
