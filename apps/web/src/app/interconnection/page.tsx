import { CanonicalModule } from '@/components/ui/CanonicalModule';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Interconnection | Land Intelligence OS',
};

export default function InterconnectionPage() {
  return <CanonicalModule title="Interconnection" />;
}
