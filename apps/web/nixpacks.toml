[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @land-intelligence/web build"]

[start]
cmd = "pnpm --filter @land-intelligence/web start"
